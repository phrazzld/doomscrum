// Word-level caption timings, generated from the whisper verification pass:
//   python3 scripts/check_script_fit.py <mp4> "<script>" --words-json <slug>.json
// then pasted here. Timestamps are seconds relative to the clip start.
import type { Word } from "./fx";

export const CAPTIONS: Record<string, Word[]> = {};
