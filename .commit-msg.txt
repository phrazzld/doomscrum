feat(legal): runtime data-egress disclosure + MIT license + AI-content disclosure — 022

Legal/safety baseline before public launch (backlog 022):

- LICENSE (MIT) added; trademark sanity check recorded in docs/LEGAL.md.
- docs/LEGAL.md: fal.ai + model + OpenRouter ToS review for marketing
  redistribution of generated clips (snapshot 2026-06-25).
- README "Legal / safety disclosure" section states AI-generated video and
  enumerates both egress payloads with source locations.
- Runtime data-egress disclosure (not just prose), the security-lane
  complement to 033:
  - doomscrum egress (CLI) prints the enumeration.
  - GET /api/egress (HTTP) returns it as JSON.
  - assets/index.html egress chip + overlay surface it in the feed UI;
    splash notes videos are AI-generated.
- src/egress.rs is the code-verified single source enumerating BOTH payloads:
  prd.raw -> OpenRouter (scriptwriter.rs:102) and spec
  title/goal/first-criterion -> fal (distill.rs). Flags the
  attacker-controlled title. Tests assert both ids, both source paths, the
  attacker-controlled note, and the UI/API surfaces.
